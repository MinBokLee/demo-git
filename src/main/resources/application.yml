spring:
  # JPA Setting
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

  #Databas Setting
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/testgit
    username: sa
    password:

